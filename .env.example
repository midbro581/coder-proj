# Coder — Corporate Training Company Profile
# 1. Copy this file to ".env" (no .example).
# 2. Update DB_PASSWORD if your MySQL server is not using the default blank password.

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=coder_db
PORT=3000